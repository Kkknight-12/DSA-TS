/**
 * 402. Remove K Digits - OPTIMAL SOLUTION
 *
 * Approach: Monotonic Increasing Stack (Greedy)
 * Time Complexity: O(n)
 * Space Complexity: O(n)
 *
 * KEY INSIGHT:
 * To get the smallest number:
 * - Smaller digits should be on the LEFT (higher place value)
 * - When we see a smaller digit, remove larger digits before it
 * - This is exactly what a Monotonic Increasing Stack does!
 *
 * POP CONDITION: stack.top > current digit
 * RESULT: Stack maintains increasing order (smallest digits first)
 */

namespace RemoveKDigitsOptimal {
    /**
     * Main function - Remove k digits to get smallest number
     *
     * @param nums - String representing a non-negative integer
     * @param k - Number of digits to remove
     * @returns Smallest possible number as string
     */
    export function removeKdigits(nums: string, k: number): string {
        // Edge Case: If we remove all digits, result is "0"
        // WHY: Empty number is represented as "0"
        if (k >= nums.length) return "0";

        // Monotonic Increasing Stack
        // WHY: We want smaller digits on the left
        // Stack stores characters (digits)
        const stack: string[] = [];

        // Traverse each digit from left to right
        // WHY: Left digits have higher place value, process them first
        for (const digit of nums) {
            // GREEDY: Pop larger digits when we see a smaller digit
            // WHY: Smaller digit on left = smaller number
            // EXAMPLE: "54" → see 4, pop 5 → "4" (4 < 54)
            //
            // Conditions to pop:
            // 1. k > 0 (we still have removals left)
            // 2. stack not empty (something to pop)
            // 3. stack.top > current (current digit is smaller)
            while (k > 0 && stack.length > 0 && stack[stack.length - 1] > digit) {
                stack.pop();
                k--;
            }

            // Push current digit to stack
            // WHY: This digit might be useful, or might be popped later
            stack.push(digit);
        }

        // IMPORTANT: Handle remaining k
        // WHY: If digits were already in ascending order, no pops happened
        // EXAMPLE: "12345", k=2 → Stack: [1,2,3,4,5], k still 2
        //          Remove from end (largest digits) → [1,2,3]
        while (k > 0) {
            stack.pop();
            k--;
        }

        // Convert stack to string and remove leading zeros
        // WHY: "0200" should become "200"
        // REGEX: ^0+ means "one or more zeros at the start"
        const result = stack.join('').replace(/^0+/, '');

        // Handle empty result
        // WHY: If all digits were zeros or removed, return "0"
        // EXAMPLE: "10", k=1 → Stack: [0] → "0" → "" after replace → return "0"
        return result === '' ? '0' : result;
    }

    /**
     * ═══════════════════════════════════════════════════════════════════════════════
     * DRY RUN - COMPLETE VISUALIZATION
     * ═══════════════════════════════════════════════════════════════════════════════
     *
     * Example 1: nums = "1432219", k = 3
     *
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │  Step  │ Digit │  Stack Before   │      Action            │ Stack After │ k │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │   1    │  '1'  │ []              │ push '1'               │ [1]         │ 3 │
     * │   2    │  '4'  │ [1]             │ 4>1, push '4'          │ [1,4]       │ 3 │
     * │   3    │  '3'  │ [1,4]           │ 3<4, pop '4', push '3' │ [1,3]       │ 2 │
     * │   4    │  '2'  │ [1,3]           │ 2<3, pop '3', push '2' │ [1,2]       │ 1 │
     * │   5    │  '2'  │ [1,2]           │ 2=2, push '2'          │ [1,2,2]     │ 1 │
     * │   6    │  '1'  │ [1,2,2]         │ 1<2, pop '2', push '1' │ [1,2,1]     │ 0 │
     * │   7    │  '9'  │ [1,2,1]         │ k=0, push '9'          │ [1,2,1,9]   │ 0 │
     * └─────────────────────────────────────────────────────────────────────────────┘
     *
     * Final Stack: [1, 2, 1, 9]
     * Result: "1219" ✓
     *
     * ═══════════════════════════════════════════════════════════════════════════════
     *
     * Example 2: nums = "541892", k = 2
     *
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │  Step  │ Digit │  Stack Before   │      Action            │ Stack After │ k │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │   1    │  '5'  │ []              │ push '5'               │ [5]         │ 2 │
     * │   2    │  '4'  │ [5]             │ 4<5, pop '5', push '4' │ [4]         │ 1 │
     * │   3    │  '1'  │ [4]             │ 1<4, pop '4', push '1' │ [1]         │ 0 │
     * │   4    │  '8'  │ [1]             │ k=0, push '8'          │ [1,8]       │ 0 │
     * │   5    │  '9'  │ [1,8]           │ k=0, push '9'          │ [1,8,9]     │ 0 │
     * │   6    │  '2'  │ [1,8,9]         │ k=0, push '2'          │ [1,8,9,2]   │ 0 │
     * └─────────────────────────────────────────────────────────────────────────────┘
     *
     * Final Stack: [1, 8, 9, 2]
     * Result: "1892" ✓
     *
     * ═══════════════════════════════════════════════════════════════════════════════
     *
     * Example 3: nums = "10200", k = 1
     *
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │  Step  │ Digit │  Stack Before   │      Action            │ Stack After │ k │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │   1    │  '1'  │ []              │ push '1'               │ [1]         │ 1 │
     * │   2    │  '0'  │ [1]             │ 0<1, pop '1', push '0' │ [0]         │ 0 │
     * │   3    │  '2'  │ [0]             │ k=0, push '2'          │ [0,2]       │ 0 │
     * │   4    │  '0'  │ [0,2]           │ k=0, push '0'          │ [0,2,0]     │ 0 │
     * │   5    │  '0'  │ [0,2,0]         │ k=0, push '0'          │ [0,2,0,0]   │ 0 │
     * └─────────────────────────────────────────────────────────────────────────────┘
     *
     * Stack to string: "0200"
     * After removing leading zeros: "200" ✓
     *
     * ═══════════════════════════════════════════════════════════════════════════════
     *
     * Example 4: nums = "1002991", k = 3
     *
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │  Step  │ Digit │  Stack Before     │      Action            │ Stack After   │ k │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │   1    │  '1'  │ []                │ push '1'               │ [1]           │ 3 │
     * │   2    │  '0'  │ [1]               │ 0<1, pop '1', push '0' │ [0]           │ 2 │
     * │   3    │  '0'  │ [0]               │ 0=0, push '0'          │ [0,0]         │ 2 │
     * │   4    │  '2'  │ [0,0]             │ 2>0, push '2'          │ [0,0,2]       │ 2 │
     * │   5    │  '9'  │ [0,0,2]           │ 9>2, push '9'          │ [0,0,2,9]     │ 2 │
     * │   6    │  '9'  │ [0,0,2,9]         │ 9=9, push '9'          │ [0,0,2,9,9]   │ 2 │
     * │   7    │  '1'  │ [0,0,2,9,9]       │ 1<9, pop '9'           │ [0,0,2,9]     │ 1 │
     * │        │       │ [0,0,2,9]         │ 1<9, pop '9'           │ [0,0,2]       │ 0 │
     * │        │       │ [0,0,2]           │ k=0, push '1'          │ [0,0,2,1]     │ 0 │
     * └─────────────────────────────────────────────────────────────────────────────┘
     *
     * Stack to string: "0021"
     * After removing leading zeros: "21" ✓
     *
     * ═══════════════════════════════════════════════════════════════════════════════
     *
     * Example 5: nums = "12345", k = 2 (Already ascending - edge case!)
     *
     * ┌─────────────────────────────────────────────────────────────────────────────┐
     * │  Step  │ Digit │  Stack Before     │      Action            │ Stack After     │ k │
     * ├─────────────────────────────────────────────────────────────────────────────┤
     * │   1    │  '1'  │ []                │ push '1'               │ [1]             │ 2 │
     * │   2    │  '2'  │ [1]               │ 2>1, push '2'          │ [1,2]           │ 2 │
     * │   3    │  '3'  │ [1,2]             │ 3>2, push '3'          │ [1,2,3]         │ 2 │
     * │   4    │  '4'  │ [1,2,3]           │ 4>3, push '4'          │ [1,2,3,4]       │ 2 │
     * │   5    │  '5'  │ [1,2,3,4]         │ 5>4, push '5'          │ [1,2,3,4,5]     │ 2 │
     * └─────────────────────────────────────────────────────────────────────────────┘
     *
     * After loop: k = 2 (no pops happened!)
     * Pop k times from end: [1,2,3,4,5] → [1,2,3]
     * Result: "123" ✓
     *
     * ═══════════════════════════════════════════════════════════════════════════════
     * EDGE CASES
     * ═══════════════════════════════════════════════════════════════════════════════
     *
     * 1. k >= nums.length: "10", k=2 → Return "0"
     * 2. All same digits: "1111", k=2 → Pop from end → "11"
     * 3. Leading zeros: "10200", k=1 → "0200" → "200"
     * 4. Result empty after regex: "0000", k=2 → "00" → "" → "0"
     * 5. Single digit: "9", k=0 → "9"
     */

    // ═══════════════════════════════════════════════════════════════════════════════
    // TEST CASES
    // ═══════════════════════════════════════════════════════════════════════════════

    export function runTests(): void {
        console.log('🧪 Testing Remove K Digits - OPTIMAL (Monotonic Increasing Stack)\n');

        const tests: Array<{ nums: string; k: number; expected: string; description: string }> = [
            {
                nums: "1432219",
                k: 3,
                expected: "1219",
                description: "LeetCode Example 1"
            },
            {
                nums: "10200",
                k: 1,
                expected: "200",
                description: "LeetCode Example 2 - Leading zeros"
            },
            {
                nums: "10",
                k: 2,
                expected: "0",
                description: "LeetCode Example 3 - Remove all"
            },
            {
                nums: "541892",
                k: 2,
                expected: "1892",
                description: "Problem example 1"
            },
            {
                nums: "1002991",
                k: 3,
                expected: "21",
                description: "Problem example 2"
            },
            {
                nums: "12345",
                k: 2,
                expected: "123",
                description: "Already ascending - pop from end"
            },
            {
                nums: "54321",
                k: 2,
                expected: "321",
                description: "Descending - pop first two"
            },
            {
                nums: "1111",
                k: 2,
                expected: "11",
                description: "All same digits"
            },
            {
                nums: "10001",
                k: 1,
                expected: "1",
                description: "Multiple leading zeros after removal"
            },
            {
                nums: "9",
                k: 0,
                expected: "9",
                description: "Single digit, no removal"
            },
            {
                nums: "100",
                k: 1,
                expected: "0",
                description: "Result is just zero"
            },
            {
                nums: "112",
                k: 1,
                expected: "11",
                description: "Pop from end (ascending)"
            }
        ];

        let passed = 0;
        let failed = 0;

        tests.forEach((test, index) => {
            const result = removeKdigits(test.nums, test.k);
            const status = result === test.expected ? '✅ PASS' : '❌ FAIL';

            if (result === test.expected) {
                passed++;
            } else {
                failed++;
            }

            console.log(`Test ${index + 1}: ${status}`);
            console.log(`  Description: ${test.description}`);
            console.log(`  Input:       nums = "${test.nums}", k = ${test.k}`);
            console.log(`  Expected:    "${test.expected}"`);
            console.log(`  Got:         "${result}"`);
            console.log();
        });

        console.log('═══════════════════════════════════════════════════════════════');
        console.log(`Total Tests: ${tests.length}`);
        console.log(`✅ Passed: ${passed}`);
        console.log(`❌ Failed: ${failed}`);
        console.log('═══════════════════════════════════════════════════════════════\n');

        if (failed === 0) {
            console.log('🎉 All tests passed! Monotonic Stack mastered! 🚀\n');
        }
    }
}

// Execute tests
RemoveKDigitsOptimal.runTests();